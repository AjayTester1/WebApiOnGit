name: Build and deploy .NET Core application to linux webapp linux332 with API Management Service aatiwarishub-apim2
on:
  push:
    branches:
    - master

env:
  AZURE_WEBAPP_NAME: linux332
  DOTNET_CORE_VERSION: 5.1.x
  WORKING_DIRECTORY: WebApiOnGit
  CONFIGURATION: Release
  AZURE_WEBAPP_PACKAGE_PATH: WebApiOnGit/publish
  AZURE_APIM_RESOURCE_PATH: /aatiwarishub-apim2/v1
  AZURE_APIM_RESOURCEGROUP: ajay
  AZURE_APIM_SERVICENAME: aatiwarishub-apim2
  AZURE_APIM_API_ID: bugbash
  AZURE_APIM_APPSERVICEURL: https://linux332.azurewebsites.net
  SWASHBUCLE_ASPNET_CORE_CLI_PACKAGE_VERSION: 5.6.3
  SWASHBUCKLE_DOTNET_CORE_VERSION: 3.1.x
  API_IMPORT_SPECIFICATION_PATH: WebApiOnGit/publish/swagger.json
  API_IMPORT_DLL: WebApiOnGit/publish/WebApiOnGit.dll  
  API_IMPORT_VERSION: v1

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
 
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: ${{ env.DOTNET_CORE_VERSION }}

    - name: Setup SwashBuckle .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: ${{ env.SWASHBUCKLE_DOTNET_CORE_VERSION }}

    - name: Restore
      run: dotnet restore ${{ env.WORKING_DIRECTORY }}

    - name: Build
      run: dotnet build ${{ env.WORKING_DIRECTORY }} --configuration ${{ env.CONFIGURATION }} --no-restore

    - name: Test
      run: dotnet test ${{ env.WORKING_DIRECTORY }} --no-build

    - name: Publish
      run: dotnet publish ${{ env.WORKING_DIRECTORY }} --configuration ${{ env.CONFIGURATION }} --no-build --output ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}

    - name: Install Swashbuckle CLI .NET Global Tool
      working-directory: ${{ env.WORKING_DIRECTORY }}
      run: dotnet tool install --global Swashbuckle.AspNetCore.Cli --version ${{ env.SWASHBUCLE_ASPNET_CORE_CLI_PACKAGE_VERSION }}

    - name: Generate Open API Specification Document
      run: swagger tofile --output "${{ env.API_IMPORT_SPECIFICATION_PATH }}" "${{ env.API_IMPORT_DLL }}" "${{ env.API_IMPORT_VERSION }}"

    - name: Publish Artifacts
      uses: actions/upload-artifact@v1.0.0
      with:
        name: webapp
        path: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:    
    - name: Download artifact from build job
      uses: actions/download-artifact@v2
      with:
        name: webapp
        path: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}

    - uses: azure/login@v1
      with:
        creds: ${{ secrets.linux332_8053 }}

    - uses: azure/appservice-settings@v1
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        general-settings-json: '{"linuxFxVersion": "DOTNETCORE|5.0"}' #'General configuration settings as Key Value pairs'

    - name: Deploy to Azure WebApp
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}

    - name: logout
      run: |
        az logout

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.aatiwarishub_apim2_spn }}

    - name: Import API into Azure API Management
      run: az apim api import --path "${{ env.AZURE_APIM_RESOURCE_PATH }}" --resource-group "${{ env.AZURE_APIM_RESOURCEGROUP }}" --service-name "${{ env.AZURE_APIM_SERVICENAME }}" --api-id "${{ env.AZURE_APIM_API_ID }}" --service-url "${{ env.AZURE_APIM_APPSERVICEURL }}" --specification-path "${{ env.API_IMPORT_SPECIFICATION_PATH }}" --specification-format OpenApi --subscription-required false

    - name: logout 
      run: | 
          az logout
